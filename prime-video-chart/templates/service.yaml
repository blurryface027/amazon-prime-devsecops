apiVersion: v1
kind: Service
metadata:
  name: {{ include "prime-video-chart.fullname" . }}
  labels:
    {{- include "prime-video-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prime-video-chart.selectorLabels" . | nindent 4 }}
